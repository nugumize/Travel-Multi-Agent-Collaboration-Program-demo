# 旅行助手 - 启动指南

## 一、启动后端（端口 8200）

打开终端，执行：

```cmd
cd /d D:\agent模型\travelAgent
.\.venv\Scripts\activate.bat
cd backend
python run.py
```

启动成功标志：看到 `✅ 配置验证通过` 和 `Application startup complete`

API 文档地址：http://localhost:8200/docs

## 二、启动前端（端口 5173）

新开一个终端，执行：

```cmd
cd /d D:\agent模型\travelAgent\frontend
npm run dev
```

启动成功标志：看到 `Local: http://localhost:5173/`

浏览器访问：http://localhost:5173/

## 三、注意事项

1. **后端终端不要关**，保持 `python run.py` 一直在运行
2. **两个终端要同时开着**，一个跑后端，一个跑前端
3. **改了 `.env` 必须手动重启后端**（不会自动热重载）
4. **改了 `.py` 文件**后端会自动热重载（reload），不用手动重启
5. **改了前端 `.vue` / `.ts` 文件**前端会自动热更新，刷新浏览器即可
6. **第一次请求会等 1-3 分钟**（初始化 MCP + 四次串行 LLM 调用）

## 四、配置文件位置

| 文件 | 作用 |
|---|---|
| `backend/.env` | 后端配置（LLM、高德 Key） |
| `frontend/.env` | 前端配置（API 地址、地图 Key） |

## 五、当前配置

| 配置项 | 值 |
|---|---|
| LLM 提供商 | ModelScope（魔搭社区） |
| LLM 模型 | `Qwen/Qwen3-235B-A22B-Instruct-2507` |
| LLM Base URL | `https://api-inference.modelscope.cn/v1/` |
| 高德 Web 服务 Key | `backend/.env` 的 `AMAP_API_KEY` |
| 高德 Web 端 JS API Key | `frontend/.env` 的 `VITE_AMAP_WEB_JS_KEY` |

## 六、常见问题

| 问题 | 原因 | 解决 |
|---|---|---|
| `ModuleNotFoundError` | 虚拟环境没激活 | 先跑 `.\.venv\Scripts\activate.bat` |
| `ERR_CONNECTION_REFUSED` 5173 | 前端没启动 | `npm run dev` |
| `400 Invalid model id` | 模型 ID 写错 | 必须带 `-2507` 后缀 |
| `401 please bind alibaba cloud` | 魔搭没绑定阿里云 | 去 modelscope.cn 绑定 + 实名认证 |
| Unsplash 401 | 没配 Unsplash Key | 正常现象，不影响功能，图片位置空白 |
| 前端 `npm run build` 报错 | TS 类型或标签错误 | 检查 `budget` 字段 6 处改动是否都正确 |

## 七、完整启动流程（从零开始）

```cmd
:: 终端 1 - 后端
cd /d D:\agent模型\travelAgent
.\.venv\Scripts\activate.bat
cd backend
python run.py

:: 终端 2 - 前端
cd /d D:\agent模型\travelAgent\frontend
npm run dev
```

然后浏览器打开 http://localhost:5173/ 即可。
