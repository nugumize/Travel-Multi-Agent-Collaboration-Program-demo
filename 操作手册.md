# 操作手册：智能旅行助手 · 后端串联与前端字段穿越

**范围**：STEP4（把 Agent 装进 Web API）+ STEP5（前端字段穿越练习）
**预计用时**：约 3 小时（含课前环境准备，不计休息约 2.5 小时）
**需要密钥**：LLM Key + 高德 Web 服务 Key（必需）｜高德 Web 端 JS API Key、Unsplash Key（可选）

---

## 目录

**课前**
- [1. 这两节课要做什么](#1-这两节课要做什么)
- [2. 课前环境准备](#2-课前环境准备)
- [3. 先认识手上这份代码](#3-先认识手上这份代码)

**第一部分 · STEP4：把 Agent 装进 Web API**
- [4. 目录结构](#4-目录结构)
- [5. 请求走过的路](#5-请求走过的路)
- [6. `TripRequest` 一个模型干三件事](#6-triprequest-一个模型干三件事)
- [7. 配置是怎么读进来的](#7-配置是怎么读进来的)
- [8. 第一步：自检（不需要密钥）](#8-第一步自检不需要密钥)
- [9. 第二步：创建并填写 `.env`](#9-第二步创建并填写-env)
- [10. 第三步：启动](#10-第三步启动)
- [11. 第四步：在 `/docs` 里发第一个请求](#11-第四步在-docs-里发第一个请求)
- [12. ⭐ 关键技能：分辨真计划和假计划](#12--关键技能分辨真计划和假计划)
- [13. 完整响应长什么样](#13-完整响应长什么样)
- [14. 另外 10 个端点（缺陷复盘素材）](#14-另外-10-个端点缺陷复盘素材)
- [15. STEP4 报错速查表](#15-step4-报错速查表)
- [16. STEP4 自检清单与思考题](#16-step4-自检清单与思考题)

**第二部分 · STEP5：前端字段穿越**
- [17. 这一节要做什么](#17-这一节要做什么)
- [18. 安装依赖并启动前端](#18-安装依赖并启动前端)
- [19. ⚠️ 最容易翻车的一个坑：端口 5173](#19-️-最容易翻车的一个坑端口-5173)
- [20. 前端代码在做什么](#20-前端代码在做什么)
- [21. 练习 B · 只读穿越（15 分钟）](#21-练习-b--只读穿越15-分钟)
- [22. 练习 A · 写入穿越（40 分钟）](#22-练习-a--写入穿越40-分钟)
- [23. 改完怎么验证](#23-改完怎么验证)
- [24. STEP5 报错速查表](#24-step5-报错速查表)
- [25. STEP5 自检清单与思考题](#25-step5-自检清单与思考题)

**附录**
- [附录：两个通用报错](#附录两个通用报错)

---

## 1. 这两节课要做什么

你手上是一个**已经写好的完整应用**：后端是四个 Agent 组成的多智能体旅行规划系统，
前端是一个 Vue 页面。

这两节课**几乎不用你从零写代码**，要做的是三件事：

1. **把它跑起来** —— 后端启动、前端启动、发出第一个真实请求
2. **看懂数据怎么流** —— 一个请求从浏览器到 LLM，经过了哪几层
3. **亲手改一个字段** —— 体会「一个字段要在 6 个地方保持一致」这件事

### 两节课的分工

| | STEP4 | STEP5 |
|---|---|---|
| 主题 | 把库代码变成 HTTP 服务 | 一个字段的完整生命周期 |
| 你动手做什么 | 跑自检、配密钥、启动、在 `/docs` 发请求 | 改 1 处（练习 B）→ 改 6 处（练习 A） |
| 里程碑 | **拿到一份完整的行程 JSON** | **亲手加一个字段并看到它影响 LLM** |
| 用时 | 约 60 分钟 | 约 75 分钟 |

### 做完之后你应该能

- 说清一个 HTTP 请求从进入到返回，经过了哪几层，每层负责什么
- 解释一个 Pydantic 模型在 API 里同时干了哪三件事
- **分辨「真计划」和「兜底假计划」** ← STEP4 最重要的技能
- 解释为什么「数据传到了后端」还不够，必须进到提示词
- 根据现象（白屏 / 字段被忽略 / 行程无变化）定位链路断在哪一层

---

## 2. 课前环境准备

⚠️ **这一节请在课前完成。** 课上现装依赖、现申请密钥，一定来不及。

### 2.1 需要装什么

| 软件 | 版本要求 | 用来做什么 |
|---|---|---|
| **Python** | **3.10 – 3.12** | 后端 |
| **Node.js** | **18 / 20 / 22 及以上** | 前端 |

⚠️ **Node 16、17、19、21 都不行。** 前端用的 `vite@6.3.6` 声明的支持范围是
`^18.0.0 || ^20.0.0 || >=22.0.0`。推荐 **20 LTS** 或 **22 LTS**。

检查：

```powershell
python --version
node -v
```

Python 去 <https://www.python.org/downloads/> 下载，安装时**务必勾选
「Add Python to PATH」**。Node 去 <https://nodejs.org/> 下载标着 **LTS** 的那个。

### 2.2 建虚拟环境

虚拟环境就是一个独立的 Python 包安装目录，让这个项目的依赖不跟你别的项目打架。
**删掉那个文件夹就等于彻底卸载**，很干净。

在**教学包根目录**（也就是本手册所在的目录）下执行：

```powershell
python -m venv .venv
```

激活它：

```powershell
# Windows PowerShell
.\.venv\Scripts\Activate.ps1

# Windows cmd
.venv\Scripts\activate.bat

# macOS / Linux
source .venv/bin/activate
```

**激活成功的标志：命令提示符前面多了一个 `(.venv)`**：

```text
(.venv) PS D:\...\智能旅行助手-教学包>
```

#### 如果 PowerShell 拒绝执行

Windows 默认禁止运行脚本，你可能看到「因为在此系统上禁止运行脚本」。
临时放开**当前这个窗口**的限制（关掉窗口就恢复，不改系统全局设置）：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

#### 确认激活的就是这一个

提示符里有 `(.venv)` 只说明激活了**某个**环境。要确认是哪一个：

```powershell
python -c "import sys; print(sys.prefix)"
```

输出应该指向你刚建的那个 `.venv`。如果指向
`C:\Users\你\AppData\Local\Programs\Python\...`，说明**没激活成功**，装包会装到全局去。

⚠️ **激活状态不会保存。** 新开终端、重启电脑都要重新激活。
课上遇到 `ModuleNotFoundError`，十有八九是忘了激活。

### 2.3 装 Python 依赖

确认提示符里有 `(.venv)`，然后在教学包根目录下：

```powershell
pip install -r backend\requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

`-i https://...tsinghua...` 是换清华镜像源，国内快很多。这一步要装十几个包，需要几分钟。

装了些什么（真正被代码用到的）：

| 包 | 干什么用 |
|---|---|
| `hello-agents[protocols]` | Agent 框架，本课主角。`[protocols]` 后缀会额外装上 MCP 支持 |
| `fastapi` | Web API 框架 |
| `uvicorn[standard]` | 运行 FastAPI 的服务器 |
| `pydantic` / `pydantic-settings` | 数据校验 / 从 `.env` 读配置 |
| `python-dotenv` | 加载 `.env` |
| `fastmcp` | MCP 协议的底层实现 |
| `uv` | 提供 `uvx` 命令，用来启动高德地图的 MCP 服务 |
| `huggingface_hub` | `hello-agents` 导入时需要它 |

（`requirements.txt` 里还有 `httpx`、`aiohttp`、`loguru`、`python-dateutil`、
`python-multipart` 五个包，**当前代码并没有用到**，装上无害。）

#### ⚠️ 两个版本陷阱

**（1）`hello-agents` 必须在 `0.2.4`–`0.2.9` 之间。**
`requirements.txt` 里写的是 `>=0.2.4,<=0.2.9`，**注意有上限**。
超出这个范围，`MCPTool` 的参数名会变（比如变成 `command` + `args`），代码直接报错。

```powershell
pip show hello-agents
```

`Version:` 那行应该在 0.2.4 到 0.2.9 之间。

**（2）`[protocols]` 这个后缀不能省。**
写成 `pip install hello-agents`（不带后缀）也能装上，但**不会**装 `fastmcp`。
后果很隐蔽：MCP 工具**不报错**，只会静默地只加载出 1 个工具。

#### 验证

```powershell
python -c "import fastapi, pydantic, uvicorn; print('web ok')"
python -c "from hello_agents import SimpleAgent, HelloAgentsLLM; from hello_agents.tools import MCPTool; print('hello-agents ok')"
```

两行都打印 `ok` 就算过。如果第二行报 `No module named 'huggingface_hub'`：

```powershell
pip install "huggingface_hub>=0.25.0" -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 2.4 ⚠️ 必做：预热 MCP 服务

第一次运行 `uvx amap-mcp-server` 时要联网下载这个包。**课堂上几十个人同时下载，
很容易失败或极慢。** 课前跑一次，让它缓存到本地：

```powershell
uvx amap-mcp-server
```

**它会一直挂着不动，这是正常的** —— MCP 服务在等你从标准输入发指令。
看到它启动起来（不再滚动下载进度）就说明包已经拉好了，按 `Ctrl+C` 退出。

之后课上再启动就是秒开。（如果提示找不到 `uvx`，先 `pip install "uv>=0.8.0"`，
装完关掉终端重开再试。）

### 2.5 前端换镜像源（国内建议做）

```powershell
npm config set registry https://registry.npmmirror.com
```

全局持久生效，只需做一次。

### 2.6 🔑 申请密钥

⚠️ **注意「高德」要申请两个 Key**，类型不同、不能互换 —— **这是本课最高频的翻车点。**

| # | 密钥 | 申请地址 | 必需？ | 给谁用 | 填到哪 |
|---|---|---|---|---|---|
| 1 | **LLM API Key** | 任选一家，如 <https://platform.deepseek.com/> | ✅ 必需 | 四个 Agent 都要调 LLM | `backend/.env` 的 `LLM_API_KEY` |
| 2 | **高德 Web 服务 Key** | <https://console.amap.com/> | ✅ 必需 | 后端查景点/天气/酒店 | `backend/.env` 的 `AMAP_API_KEY` |
| 3 | **高德 Web 端（JS API）Key** | <https://console.amap.com/> | ⭕ 可选* | 前端结果页画地图 | `frontend/.env` 的 `VITE_AMAP_WEB_JS_KEY` |
| 4 | **Unsplash Access Key** | <https://unsplash.com/developers> | ⭕ 可选 | 结果页景点配图 | `backend/.env` 的 `UNSPLASH_ACCESS_KEY` |

\* 第 3 个不配也能跑完全部课程内容，只是结果页地图空白。

#### LLM API Key

代码兼容 OpenAI 接口格式，所以 DeepSeek、通义、Kimi、ModelScope、OpenAI 都可以。
以 DeepSeek 为例：注册登录 → 左侧「API keys」→「创建 API key」→ **立刻复制保存**
（页面关掉就再也看不到完整的 key 了）。

你需要记下三个值：

| 变量 | DeepSeek 的值 |
|---|---|
| `LLM_API_KEY` | `sk-` 开头的那串 |
| `LLM_BASE_URL` | `https://api.deepseek.com/v1` |
| `LLM_MODEL_ID` | 例如 `deepseek-chat` |

⚠️ **确认账户里有余额。** 免费额度用尽时报错信息往往很含糊（见 §12 —— 这种情况下
接口仍会返回 200 和一份**编造的**行程）。

#### 高德地图的两个 Key

1. 打开 <https://console.amap.com/> 注册登录
2. 「应用管理」→「我的应用」→「创建新应用」
3. 在这个应用下点「添加 Key」，**加两次**，分别选不同的「服务平台」：

| 你需要的 | 创建时「服务平台」选 | 给谁用 | 填到哪个变量 |
|---|---|---|---|
| 第一个 Key | **Web 服务** | 后端（Python）查景点、天气、酒店 | `AMAP_API_KEY` |
| 第二个 Key | **Web 端（JS API）** | 浏览器里的地图组件 | `VITE_AMAP_WEB_JS_KEY` |

**记牢这个区别：**

- **Web 服务** = 给服务器程序调用 HTTP 接口用的 → STEP4 用它
- **Web 端（JS API）** = 给浏览器里的 JavaScript 地图组件用的 → STEP5 用它

**配错的典型症状：「工具能加载出来，但每次查询都返回空/失败」。**
报错信息**不会**告诉你类型选错了。遇到这个现象，第一件事就是回来确认后端用的是
**Web 服务** 类型。

> 🔒 Web 端 JS API 的 Key 设计上就会暴露在浏览器里，这是正常的。
> 但**务必在高德控制台给它配域名白名单**，否则别人抄走你的 Key 就能刷你的额度。

#### Unsplash Access Key（可选）

只用于结果页的景点配图，不配也能完成全部课程内容。
<https://unsplash.com/developers> → 注册 →「Your apps」→「New Application」→
复制 **Access Key**。

📌 **`UNSPLASH_SECRET_KEY` 不用申请也不用填。** Unsplash 会同时给你两个 Key，
但**本项目全代码库从未读取过 Secret Key**。

⚠️ Unsplash 是境外服务，国内网络可能不通。连不上就跳过。

### 2.7 课前检查清单

- [ ] `python --version` 在 3.10–3.12
- [ ] `node -v` ≥ 18
- [ ] 教学包根目录下有 `.venv`，激活后提示符显示 `(.venv)`
- [ ] `python -c "import fastapi, pydantic, uvicorn; print('web ok')"` 通过
- [ ] `python -c "from hello_agents.tools import MCPTool; print('ok')"` 通过
- [ ] `pip show hello-agents` 版本在 0.2.4–0.2.9
- [ ] `uvx amap-mcp-server` 跑过一次（已缓存）
- [ ] LLM 的三项值已拿到，且账户**有余额**
- [ ] 高德 **Web 服务** Key 已拿到
- [ ] （可选）高德 **Web 端 JS API** Key、Unsplash Access Key

---

## 3. 先认识手上这份代码

这两节课不从零写模型和 Agent —— 它们已经写好了。但你必须知道它们是什么，
否则 STEP4 的「接线」和 STEP5 的「第 6 处改动」都会变成照抄。

### 3.1 三层结构

```text
┌─ API 层 ────────── app/api/         FastAPI 路由，把 HTTP 请求转成函数调用
├─ 业务层 ────────── app/agents/      四个 Agent 协作生成行程
└─ 模型层 ────────── app/models/      Pydantic 模型，定义数据长什么样
```

### 3.2 模型层：Pydantic 在拦 LLM 的胡说

`app/models/schemas.py` 定义了整套数据结构：`TripRequest`（请求）、
`TripPlan`（完整行程）、`DayPlan`（一天）、`Attraction`（景点）、`Hotel`、`Meal`、
`WeatherInfo`、`Budget`。

它们的作用不是「类型注解好看」，而是**契约**：

- LLM 返回的 JSON 必须能塞进这些模型，塞不进就报错
- 前端发来的请求必须符合 `TripRequest`，不符合直接 422
- 里面还有 `field_validator`，比如把工具返回的 `"30°C"` 转成整数 `30`

📌 这一层在 §6 会详细讲，因为它是 STEP4 最值得学的地方。

### 3.3 业务层：四个 Agent 顺序协作

`app/agents/trip_planner_agent.py` 里的 `MultiAgentTripPlanner` 是主角。
它在 `__init__` 里创建了**一个共享的 MCP 工具**和**四个 Agent**：

```text
共享的 MCPTool (uvx amap-mcp-server) ← 高德地图的一堆工具
        │
        ├── 景点搜索专家  attraction_agent  （挂了工具）
        ├── 天气查询专家  weather_agent     （挂了工具）
        ├── 酒店推荐专家  hotel_agent       （挂了工具）
        └── 行程规划专家  planner_agent     （不需要工具，只做整合）
```

`plan_trip()` 里是**严格串行**的四步（`trip_planner_agent.py:225` 起）：

```text
步骤1 搜索景点  → 步骤2 查询天气  → 步骤3 搜索酒店  → 步骤4 生成行程计划
   ↓                ↓                 ↓                  ↓
 调 LLM+工具     调 LLM+工具       调 LLM+工具        只调 LLM（整合前三步）
```

⏱️ **四次 LLM 调用是串行的，时间是四段相加。** 这就是为什么一个请求要几十秒到几分钟。

三个要点：

**（1）MCP 工具是共享的，只创建一次**（`:170-177`）。
`MCPTool` 会用 `uvx` 起一个子进程。如果每个 Agent 各建一个，机器上就会堆四个子进程。

**（2）环境变量在这里改了名**（`:174`）：

```python
env={"AMAP_MAPS_API_KEY": settings.amap_api_key}
```

你在 `.env` 里填的是 `AMAP_API_KEY`，传给 MCP 子进程时变成 **`AMAP_MAPS_API_KEY`**
（多了 `MAPS`）。**两个名字都对，只是用在不同的地方。**

**（3）提示词里写死了工具调用格式。** 比如景点 Agent 的提示词要求 LLM 回复
`[TOOL_CALL:amap_maps_text_search:keywords=...,city=...]`。这是本项目让 LLM 调工具的方式。

#### ⚠️ 这里有一个值得记住的缺陷

看步骤 2（`trip_planner_agent.py:252`）：

```python
weather_query = f"请查询{request.city}的天气信息"
```

**它没有把日期传进去。** 高德的天气工具返回的是「查询当天起的预报」，
但生成的 `weather_info` 里贴的是你的旅行日期标签。

**结果：日期标签是对的，温度数字却来自查询当天。** 如果你查的是三个月后的行程，
这份天气毫无意义 —— 而它看起来完全正常。这是本课最值得记住的一个缺陷。

**（4）失败会被兜底掉。** `plan_trip()` 的 `except` 分支调用
`_create_fallback_plan()`（`:281`），**悄悄返回一份编造的计划**而不是抛错。
这一点非常重要，§12 专门讲。

### 3.4 与课本文档的差异

⚠️ **课本 `ch13.md` 里的代码和你手上这份不一致。照课本敲会卡住，以手上的代码为准。**

和这两节课最相关的四条：

| 课本文档写的 | 实际代码 |
|---|---|
| 天数字段 `days` | **`travel_days`** |
| `preferences` 是 `str` | **`List[str]`**（字符串列表） |
| 请求里有 `budget` | **没有这个字段** ← STEP5 练习 A 就是把它补上 |
| （没提） | 多一个 `free_text_input` ← STEP5 练习 B 用它 |

`自检.py` 会自动把这四条对照打印出来。

---

# 第一部分 · STEP4：把 Agent 装进 Web API

前面那些代码都是**库代码** —— 只能自己跑脚本调用。这一部分把它变成一个
**能被浏览器和前端调用的 HTTP 服务**。

**这一部分几乎不用你写代码。** 你要做的是读懂它、跑起来、发出第一个真实请求，
并学会在出问题时定位。

---

## 4. 目录结构

```text
智能旅行助手-教学包/
├── 操作手册.md              ← 你正在读的
├── 自检.py                  ← ⭐ 不用密钥的装配检查
├── .gitignore
├── .venv/                   ← §2.2 你自己建的
├── backend/
│   ├── run.py               ← ⭐ 启动入口
│   ├── requirements.txt
│   ├── .env.example         ← 配置模板（§9 据此创建 .env）
│   └── app/
│       ├── __init__.py
│       ├── config.py        ← 配置读取 + 校验
│       ├── models/
│       │   ├── __init__.py
│       │   └── schemas.py   ← ⭐ 全部数据模型
│       ├── agents/
│       │   ├── __init__.py
│       │   └── trip_planner_agent.py   ← ⭐ 四个 Agent + MCP
│       ├── services/
│       │   ├── __init__.py
│       │   ├── llm_service.py          ← LLM 单例 + 429 重试
│       │   ├── amap_service.py         ← ⚠️ 大部分是桩代码，见 §14
│       │   └── unsplash_service.py
│       └── api/
│           ├── __init__.py
│           ├── main.py      ← ⭐ FastAPI 主应用
│           └── routes/
│               ├── __init__.py
│               ├── trip.py  ← ⭐ 核心：POST /api/trip/plan
│               ├── poi.py
│               └── map.py
└── frontend/                ← 第二部分用
    ├── package.json
    ├── .env.example
    └── src/
        ├── main.ts
        ├── App.vue
        ├── env.d.ts
        ├── types/index.ts   ← ⭐ 练习 A 第 1 处
        ├── services/api.ts
        └── views/
            ├── Home.vue     ← ⭐ 练习 A 第 2、3、4 处
            └── Result.vue
```

那些 `__init__.py` 看着没用，但**少一个就会 `ModuleNotFoundError`** —— 它们是让 Python
把目录当成「包」的标记。**别删。**

运行过之后还会出现 `__pycache__/` 目录，那是 Python 的缓存，**不是你弄坏了什么**，忽略即可。

---

## 5. 请求走过的路

**这张图是 STEP4 的核心。** 请求从上到下走一遍，再从下到上返回：

```text
  浏览器 /docs 或前端
        │  POST /api/trip/plan  { "city": "北京", ... }
        ▼
  ┌─────────────────────────────────────────────────┐
  │ main.py   FastAPI 应用                          │
  │   · CORS 中间件（决定哪些网页域名可以调用）        │
  │   · include_router(trip.router, prefix="/api")  │
  └─────────────────────────────────────────────────┘
        ▼
  ┌─────────────────────────────────────────────────┐
  │ trip.py   路由层                                │
  │   · TripRequest 校验入参 → 不合法直接返回 422    │
  │   · get_trip_planner_agent()  拿到单例          │
  │   · agent.plan_trip(request)                    │
  │   · 包装成 TripPlanResponse                     │
  └─────────────────────────────────────────────────┘
        ▼
  ┌─────────────────────────────────────────────────┐
  │ trip_planner_agent.py   业务层                   │
  │   景点Agent → 天气Agent → 酒店Agent → 规划Agent  │
  │                    ↓                            │
  │            共享的 MCPTool                        │
  └─────────────────────────────────────────────────┘
        ▼
   uvx amap-mcp-server 子进程 → 高德 HTTP API
```

### 5.1 路径是怎么拼出来的

`POST /api/trip/plan` 这个路径由**三段拼接**而成。理解这点能省掉很多
「404 找不到接口」的困惑：

| 位置 | 代码 | 贡献的片段 |
|---|---|---|
| `main.py:30` | `app.include_router(trip.router, prefix="/api")` | `/api` |
| `trip.py:11` | `router = APIRouter(prefix="/trip", ...)` | `/trip` |
| `trip.py:15` | `@router.post("/plan", ...)` | `/plan` |

三段拼起来 = `/api/trip/plan`。

### 5.2 响应外面套了一层

Agent 返回的是 `TripPlan`，但接口返回的**不是它本身**（`trip.py:48-52`）：

```python
return TripPlanResponse(
    success=True,
    message="旅行计划生成成功",
    data=trip_plan
)
```

所以 HTTP 响应体长这样：

```json
{
  "success": true,
  "message": "旅行计划生成成功",
  "data": { "city": "北京", "days": [...], ... }
}
```

**真正的旅行计划在 `data` 这个键里面。** 前端要从这里往里取 —— 第二部分会遇到。

📌 前端代码里写的是 `response.data.data`，那是从 axios 的角度说的：axios 把整个 HTTP
响应体放在它自己的 `response.data` 上，所以再往里一层才是我们这个 `data`。
**HTTP 响应体里就是一层 `data`。**

### 5.3 单例模式

`get_trip_planner_agent()`（`trip_planner_agent.py:443`）用一个全局变量保证
`MultiAgentTripPlanner` **只初始化一次**：

```python
_multi_agent_planner = None

def get_trip_planner_agent() -> MultiAgentTripPlanner:
    global _multi_agent_planner
    if _multi_agent_planner is None:
        _multi_agent_planner = MultiAgentTripPlanner()
    return _multi_agent_planner
```

**想一想为什么必须是单例**：每次初始化都会 `uvx` 起一个 MCP 子进程。
如果每个请求都新建一个，几次请求下来机器上就堆满了子进程。

📌 **注意单例是「第一次请求时」创建的，不是启动时。** 这解释了为什么第一个请求特别慢
（见 §11.2）。

---

## 6. `TripRequest` 一个模型干三件事

这是 FastAPI 最值得学的一点。`trip.py:20` 只写了一个类型标注：

```python
async def plan_trip(request: TripRequest):
```

这一个标注同时带来了三件事：

| 作用 | 表现 |
|---|---|
| **① 自动校验** | 字段缺失、类型不对、超出范围 → **自动返回 422**，你的函数体根本不会执行 |
| **② 自动文档** | `/docs` 里那个可填写的表单，是从这个模型生成的 |
| **③ 类型提示** | 函数体里写 `request.` 编辑器会自动补全字段名 |

那些看起来「只是声明字段」的模型，到这一层才显出真正的价值：
**它们是前后端之间的合同（contract）。**

### 6.1 `TripRequest` 的真实定义

`backend/app/models/schemas.py:10-19`：

```python
class TripRequest(BaseModel):
    """旅行规划请求"""
    city: str = Field(..., description="目的地城市", example="北京")
    start_date: str = Field(..., description="开始日期 YYYY-MM-DD", example="2025-06-01")
    end_date: str = Field(..., description="结束日期 YYYY-MM-DD", example="2025-06-03")
    travel_days: int = Field(..., description="旅行天数", ge=1, le=30, example=3)
    transportation: str = Field(..., description="交通方式", example="公共交通")
    accommodation: str = Field(..., description="住宿偏好", example="经济型酒店")
    preferences: List[str] = Field(default=[], description="旅行偏好标签", example=["历史文化", "美食"])
    free_text_input: Optional[str] = Field(default="", description="额外要求", example="希望多安排一些博物馆")
```

三个容易踩的点：

- **`travel_days` 有硬性范围 `ge=1, le=30`。** 填 `0` 或 `40` 都会返回 422，
  而错误信息只会说这个字段不合法，**不会提醒你「范围是 1 到 30」**。
- **`preferences` 和 `free_text_input` 是可选的**（有 `default`），其余 6 个都是必填。
- **没有 `budget` 字段。** 这是 STEP5 练习 A 要加的东西。

---

## 7. 配置是怎么读进来的

### 7.1 `.env` 到底从哪被加载

这是最容易产生误解的地方。`config.py` 里有**三个**加载点：

```python
# config.py:11
load_dotenv()
```

这一行**不看你在哪个目录运行**。python-dotenv 会从 `config.py` 自己所在的位置
（`backend/app/`）逐层往上找 `.env`，于是找到 `backend/.env`。

📌 **结论：`.env` 会被正确加载，和你的当前目录无关。**
（但 `run.py` 还是建议在 `backend/` 下跑，路径最短、最不容易出岔子。）

```python
# config.py:49-50
class Config:
    env_file = ".env"
```

这是 pydantic-settings 的机制，它**才是**依赖当前目录的。但因为上面那行已经把变量
塞进 `os.environ` 了，这里是否命中都不影响结果。

```python
# config.py:14-16 —— 第三个加载点，容易造成困惑
helloagents_env = Path(__file__).parent.parent.parent.parent / "HelloAgents" / ".env"
if helloagents_env.exists():
    load_dotenv(helloagents_env, override=False)
```

如果**教学包的上一级目录**里正好有一个 `HelloAgents/` 仓库，它的 `.env` 会被
**静默合并**进来。正常情况下没有这个目录，但排查「我明明没配这个变量」时要想到它。

### 7.2 哪些变量真的被用了

⚠️ **这张表能省掉大量无效排查：**

| `.env` 里的变量 | 谁读它 | 有用吗 |
|---|---|---|
| `AMAP_API_KEY` | `config.py:35` → 传给 MCP 子进程 | ✅ **启动时强制校验** |
| `LLM_API_KEY` | `HelloAgentsLLM()` 自己读 | ✅ 必需（但启动时只警告） |
| `LLM_BASE_URL` | `HelloAgentsLLM()` 自己读 | ✅ 必需 |
| `LLM_MODEL_ID` | `HelloAgentsLLM()` 自己读 | ✅ 必需 |
| `HOST` / `PORT` | `config.py:28-29` | ✅ |
| `CORS_ORIGINS` | `config.py:32` | ✅ 第二部分会用到 |
| `UNSPLASH_ACCESS_KEY` | `unsplash_service.py:13` | 🟡 只影响景点配图 |
| `LOG_LEVEL` | 只在启动横幅里打印 | 🟡 |
| `LLM_TIMEOUT` | **没有任何代码读它** | ❌ 无效 |
| `UNSPLASH_SECRET_KEY` | **没有任何代码读它** | ❌ 无效，不用申请 |

📌 **注意 `LLM_*` 这四个变量不是 `Settings` 类的字段。** `config.py:52` 的
`extra = "ignore"` 让它们被忽略掉 —— 它们只通过 `os.environ` 到达 `HelloAgentsLLM`。

（这个 `extra = "ignore"` 是**必需的**，不是可有可无：没有它，`.env` 里这些
「没有对应字段」的变量会直接让程序启动失败。）

### 7.3 启动校验：只有高德 Key 是硬要求

`config.py:74-80`：

```python
if not settings.amap_api_key:
    errors.append("AMAP_API_KEY未配置")

llm_api_key = os.getenv("LLM_API_KEY") or os.getenv("OPENAI_API_KEY")
if not llm_api_key:
    warnings.append("LLM_API_KEY或OPENAI_API_KEY未配置,LLM功能可能无法使用")
```

区别很重要：

- **高德 Key 缺失 → 报错 → 启动失败。**
- **LLM Key 缺失 → 只警告 → 照样启动**，然后**在第一次请求时才失败**。

⏰ **校验发生的时机也要留意**：`validate_config()` 在 `main.py:47` 的**启动事件**里
调用，不是在进程启动前。所以你会先看到 uvicorn 自己的日志，**之后**才看到配置错误，
最后是 `Application startup failed. Exiting.`

---

## 8. 第一步：自检（不需要密钥）

在碰密钥之前，先确认「装配」是对的。

在教学包根目录下，确认虚拟环境已激活（提示符里有 `(.venv)`），然后：

```powershell
python .\自检.py
```

📌 **这个脚本按自己所在的位置去找 `backend/app`，和你的当前目录无关。**
所以下面两种写法都能跑：

```powershell
python .\自检.py          # 在教学包根目录下
python ..\自检.py         # 在 backend\ 下
```

唯一的要求是：`自检.py` 必须和 `backend/` 保持同级（也就是留在教学包根目录）。

### 8.1 期望输出

```text
============================================================
STEP4 自检 · 后端装配检查
============================================================

[1/4] 依赖检查
  ✅ fastapi  (fastapi 0.141.1)
  ✅ pydantic 2.x  (pydantic 2.13.4)
  ✅ hello-agents + MCPTool  (hello-agents 0.2.9)
  ✅ uvicorn  (uvicorn 0.52.1)

[2/4] 模块导入
  ✅ app.models.schemas  (schemas)
  ✅ app.config  (port=8200)
  ✅ app.agents.trip_planner_agent  (trip_planner_agent)
  ✅ app.api.main  (FastAPI app)

[3/4] 路由注册
  ✅ 核心端点存在  (POST /api/trip/plan)
     已注册的端点:
       GET      /
       GET      /api/map/health
       GET      /api/map/poi
       POST     /api/map/route
       GET      /api/map/weather
       GET      /api/poi/detail/{poi_id}
       GET      /api/poi/photo
       GET      /api/poi/search
       GET      /api/trip/health
       POST     /api/trip/plan
       GET      /health
  ✅ 端点清单  (共 11 个)

[4/4] 请求模型字段（对照课本文档的差异）
  ✅ TripRequest 字段齐全  (8 个字段)
     文档写的        实际代码
       days      ->  travel_days     ✅
       budget    ->  (无此字段)       ✅
       (未提)     ->  free_text_input ✅
       str       ->  List[str]       ✅
  ✅ 与文档的差异

============================================================
✅ 全部通过 (12/12)
============================================================
```

版本号会因人而异，**只要都是 ✅ 就行**。总共 **12** 项检查（`[1/4]` 是分组编号，不是总数）。

失败时打印 `❌ N 项失败 / M 项`，退出码为 1。

⚠️ **自检通过 ≠ 能启动。** 自检**不检查密钥**（它自己塞了个占位值），
而真正启动时 `AMAP_API_KEY` 是硬性要求。**别把「12/12 通过」当成「run.py 一定能起来」。**

📌 做完练习 A 之后再跑自检，`budget -> (无此字段)` 那一行会变成 `⚠️ 已被添加`。
**这是正常的**，说明你改对了。

---

## 9. 第二步：创建并填写 `.env`

后端的密钥和端口都从 `backend/.env` 读。这个文件**不在包里**（密钥是私有的），
需要你从模板复制一份：

```powershell
cd backend
copy .env.example .env
notepad .env
```

要填的四项：

```ini
LLM_MODEL_ID=你的模型名
LLM_API_KEY=你的LLM密钥
LLM_BASE_URL=你的LLM服务地址
AMAP_API_KEY=你的高德Web服务Key
```

（密钥去哪申请见 §2.6。`CORS_ORIGINS`、`HOST`、`PORT` 模板里已经填好了合理的默认值，
不用改。）

📌 注意 `.env` 里写的是 `AMAP_API_KEY`（不带 `MAPS`）。代码在传给 MCP 子进程时会
自动改名成 `AMAP_MAPS_API_KEY`（`trip_planner_agent.py:174`）。
**两个名字都对，只是用在不同的地方** —— §3.3 讲过这个转换。

⚠️ **`.env` 里是你的真实密钥，不要提交到代码仓库、也不要发给别人。**
（教学包根目录的 `.gitignore` 已经帮你挡住了。）

---

## 10. 第三步：启动

```powershell
cd backend
python run.py
```

`run.py` 全文只有这么点内容：

```python
uvicorn.run(
    "app.api.main:app",
    host=settings.host,
    port=settings.port,
    reload=True,
    log_level=settings.log_level.lower()
)
```

（`python run.py` 能跑通，是因为 Python 会把 `run.py` 所在的目录放进模块搜索路径，
所以 `from app.config import ...` 找得到 `app` 包。这也是为什么建议在 `backend/` 下启动。）

### 10.1 期望输出

```text
INFO:     Will watch for changes in these directories: ['...\backend']
INFO:     Uvicorn running on http://0.0.0.0:8200 (Press CTRL+C to quit)
INFO:     Started reloader process [51292] using WatchFiles
...\app\api\routes\map.py:24: FastAPIDeprecationWarning: `example` has been deprecated, please use `examples` instead
  keywords: str = Query(..., description="搜索关键词", example="故宫"),
...（还有两条类似的 warning）
INFO:     Started server process [75108]
INFO:     Waiting for application startup.

============================================================
🚀 智能旅行助手 v1.0.0
============================================================
应用名称: 智能旅行助手
版本: 1.0.0
服务器: 0.0.0.0:8200
高德地图API Key: 已配置
LLM API Key: 已配置
LLM Base URL: <你在 .env 里填的 LLM_BASE_URL>
LLM Model: <你在 .env 里填的 LLM_MODEL_ID>
日志级别: INFO

✅ 配置验证通过

============================================================
📚 API文档: http://localhost:8200/docs
📖 ReDoc文档: http://localhost:8200/redoc
============================================================

INFO:     Application startup complete.
```

**要确认的三行：**

1. `高德地图API Key: 已配置`
2. `LLM API Key: 已配置`
3. `✅ 配置验证通过`

⚠️ 那几条 `FastAPIDeprecationWarning` 是**正常的**，来自框架内部对旧写法的提醒，
不影响运行。（自检脚本把警告过滤掉了，所以自检看着比启动干净 —— 不是你启动时弄坏了什么。）

📌 `0.0.0.0` 表示「监听所有网卡」，**不是**你要在浏览器里输入的地址。
浏览器请用 `http://localhost:8200`。

### 10.2 `reload=True` 的两面

保存任何 `.py` 文件，服务会自动重启 —— 课堂上改代码很方便。

**代价**：每次重启都会把 Agent 单例连同它的 MCP 子进程一起丢掉，
下一次请求要重新初始化一遍（又是十几秒到一分钟）。演示时如果不想被打断，可以先别改文件。

📌 **改 `.env` 不会触发 reload**（它不是 `.py` 文件）。改了配置必须手动重启。

---

## 11. 第四步：在 `/docs` 里发第一个请求

浏览器打开：

```text
http://localhost:8200/docs
```

这个页面标题是 **「智能旅行助手 1.0.0」**，由 FastAPI 根据 `main.py:12-18` 自动生成。
你会看到全部 11 个端点，按 `tags` 分组。

📌 先打开 `http://localhost:8200/` 也可以，但它只返回一小段 JSON，**不是**操作界面。
要发请求请去 `/docs`。

### 11.1 操作步骤

1. 找到 **`POST /api/trip/plan`**，点一下展开
2. 点右侧 **`Try it out`** 按钮
3. 请求体已经预填好示例（来自 `schemas.py:22-33` 的 `json_schema_extra`），可以直接用：

```json
{
  "city": "北京",
  "start_date": "2025-06-01",
  "end_date": "2025-06-03",
  "travel_days": 3,
  "transportation": "公共交通",
  "accommodation": "经济型酒店",
  "preferences": ["历史文化", "美食"],
  "free_text_input": "希望多安排一些博物馆"
}
```

4. 点 **`Execute`**
5. **等着。**

### 11.2 关于等待时间

⏱️ **第一次成功请求可能要 2 分半（实测 153 秒）。** 请把浏览器标签留着，别急着刷新。

慢在两处：

1. **Agent 单例是第一次请求时才创建的**（不是启动时），所以第一次请求要额外承担
   启动 `uvx amap-mcp-server` 子进程 + MCP 握手的时间。
2. **四个 Agent 顺序调用 LLM**，其中三个还要等 MCP 工具往返。这是串行的，
   时间是四段相加。

从第二次请求开始会快一些（省掉初始化），但**四次 LLM 调用的时间省不掉**。
具体耗时取决于你用的模型 —— 快的模型可能 30–60 秒，慢的模型两三分钟都正常。

⚠️ 如果超过 5 分钟还没响应，去终端看是不是卡在某个步骤上（见 §15 的 E13/E14）。

同时去看终端，你应该看到这样的日志：

```text
============================================================
📥 收到旅行规划请求:
   城市: 北京
   日期: 2025-06-01 - 2025-06-03
   天数: 3
============================================================

🔄 获取多智能体系统实例...
🚀 开始生成旅行计划...
🚀 开始多智能体协作规划旅行...
📍 步骤1: 搜索景点...
🌤️  步骤2: 查询天气...
🏨 步骤3: 搜索酒店...
📋 步骤4: 生成行程计划...
✅ 旅行计划生成完成!
✅ 旅行计划生成成功,准备返回响应
```

⚠️ **请求处理期间整个服务是卡住的。** `trip.py:44` 调用的 `agent.plan_trip()` 是同步的，
会阻塞事件循环。所以你在另一个标签页刷 `/docs` 会觉得网页没反应 ——
**这是正常现象，不是崩了。**

---

## 12. ⭐ 关键技能：分辨真计划和假计划

**这是 STEP4 最重要的一节，请务必读完。**

`trip_planner_agent.py` 里有一个兜底机制：任何环节抛异常，都会**悄悄返回一份编造的计划**，
而不是把错误抛出去。

于是会出现这种情况：

- HTTP 状态码 **200**
- 响应里 **`"success": true`**
- `message` 是 **「旅行计划生成成功」**
- 但里面的内容**全是假的**

### 12.1 怎么识别假计划

看 `data.days[].attractions[].name`：

| 看到的景点名 | 结论 |
|---|---|
| `故宫博物院`、`天坛公园`…… | ✅ 真计划 |
| **`北京景点1`、`北京景点2`** | ❌ 兜底假计划，中间某一步失败了 |

`北京景点1` 这种名字来自 `trip_planner_agent.py:412`：

```python
name=f"{request.city}景点{j+1}"
```

**只要看到「城市名 + 景点 + 数字」，就说明这是编的。**

另外两个辅助特征：兜底计划的 `budget` 和 `weather_info` 一定是空的
（`"budget": null`、`"weather_info": []`）。

### 12.2 看到假计划怎么排查

**去终端看，不要盯着浏览器。** 终端里才有真正的异常堆栈。

先搜这几个关键词：

```text
❌ 生成旅行计划失败:
⚠️  解析响应失败:
429
insufficient_quota
```

常见原因（按出现频率排）：

1. **LLM 额度用尽或被限流** —— 终端会有 `⚠️ LLM rpm exhausted, retry n/3`
   （来自 `llm_service.py` 的重试代理，会自动重试 3 次），最后
   `❌ 生成旅行计划失败: LLM调用失败: Error code: 429 ... insufficient_quota`。
   **这是实测中最常撞到的一种，而接口照样返回 200。**
2. `LLM_BASE_URL` 或 `LLM_MODEL_ID` 配错了
3. 高德 Key 不是「Web 服务」类型
4. `uvx` 拉包失败（网络问题）
5. LLM 返回的 JSON 格式解析不了（会看到 `⚠️  解析响应失败:`）

### 12.3 关于 `budget` 的一个提醒

`TripPlan` 里的 `budget` 是**可选**的（`schemas.py`：`Optional[Budget] = Field(default=None)`），
所以正常响应里也可能是 `"budget": null` —— 那**不一定**代表失败。

它**不可能**是 `{}`。如果被填充，会是一个包含 5 个整数字段的对象：
`total_attractions`、`total_hotels`、`total_meals`、`total_transportation`、`total`。

---

## 13. 完整响应长什么样

下面是**一次真实成功请求的响应**（为了篇幅每天只留了 1 个景点和 1 顿饭）：

```json
{
  "success": true,
  "message": "旅行计划生成成功",
  "data": {
    "city": "北京",
    "start_date": "2026-09-01",
    "end_date": "2026-09-03",
    "days": [
      {
        "date": "2026-09-01",
        "day_index": 0,
        "description": "首日探访皇家宫殿与历史地标",
        "transportation": "地铁+步行",
        "accommodation": "经济型酒店",
        "hotel": {
          "name": "京彩青年酒店(回龙观霍营地铁站店)",
          "address": "科星西路106号",
          "location": { "longitude": 116.26, "latitude": 40.08 },
          "price_range": "300-500元",
          "rating": "4.2",
          "distance": "距故宫约15公里",
          "type": "经济型酒店",
          "estimated_cost": 400
        },
        "attractions": [
          {
            "name": "故宫博物院",
            "address": "景山前街4号",
            "location": { "longitude": 116.3967, "latitude": 39.9141 },
            "visit_duration": 180,
            "description": "明清皇宫建筑群，世界文化遗产",
            "category": "历史文化",
            "rating": null,
            "photos": [],
            "poi_id": "",
            "image_url": null,
            "ticket_price": 60
          }
        ],
        "meals": [
          {
            "type": "breakfast",
            "name": "老北京豆汁配焦圈",
            "address": null,
            "location": null,
            "description": "传统北京风味早餐",
            "estimated_cost": 30
          }
        ]
      }
    ],
    "weather_info": [
      {
        "date": "2026-09-01",
        "day_weather": "雷阵雨",
        "night_weather": "雷阵雨",
        "day_temp": 30,
        "night_temp": 23,
        "wind_direction": "西风",
        "wind_power": "1-3级"
      }
    ],
    "overall_suggestions": "雨天备伞防雨，早晚温差达10°C需添衣。优先预约故宫/国博门票...",
    "budget": {
      "total_attractions": 215,
      "total_hotels": 1200,
      "total_meals": 780,
      "total_transportation": 210,
      "total": 2405
    }
  }
}
```

**具体内容由 LLM 生成，每次都不一样**，但结构应该完全一致。

对照检查这几处，就能确认是真实计划：

- 景点名是**真的**（`故宫博物院`，不是 `北京景点1`）
- 酒店名带具体门店（`京彩青年酒店(回龙观霍营地铁站店)`）—— 这是高德查回来的
- `budget` **不是 `null`**，`total` 是各项之和
- `weather_info` 有 `travel_days` 条，且 `day_temp` 是**整数**

结构速查：

- `TripPlan` 的 7 个字段：`city`、`start_date`、`end_date`、`days`、`weather_info`、
  `overall_suggestions`、`budget`
- `DayPlan` 的 8 个字段：`date`、`day_index`（**从 0 开始**）、`description`、
  `transportation`、`accommodation`、`hotel`、`attractions`、`meals`

📌 `day_temp` 是**整数 `30`**，而工具返回的原始数据是 `30°C` —— 这正是 `schemas.py` 里
那个 `field_validator(mode='before')` 的功劳。**模型不只是声明，它在做转换。**

⚠️ 但这份天气有个隐藏问题：**它的数据其实来自错误的日期。**
日期标签是对的，数字却是查询当天的 —— 原因见 §3.3 的那个缺陷说明。

---

## 14. 另外 10 个端点（缺陷复盘素材）

课程主线只用 `POST /api/trip/plan`。其余端点的真实状态如下，
**列出来是为了避免你误以为自己配错了**：

| 端点 | 真实状态 |
|---|---|
| `GET /` | ✅ 返回服务基本信息 |
| `GET /health` | ✅ **能用**，返回 `{"status": "healthy", ...}` |
| `POST /api/trip/plan` | ✅ **本节主角** |
| `GET /api/trip/health` | ❌ **必然返回 503**，见 §14.1 |
| `GET /api/map/poi` | ⚠️ 永远返回 `"data": []`（代码里写死的） |
| `GET /api/map/weather` | ⚠️ 同上，永远空数组 |
| `POST /api/map/route` | ❌ **必然返回 500**，见 §14.2 |
| `GET /api/map/health` | ✅ 能用 |
| `GET /api/poi/search` | ⚠️ 永远返回 `"data": []` |
| `GET /api/poi/detail/{poi_id}` | ⚠️ 依赖同样的桩代码 |
| `GET /api/poi/photo?name=故宫` | 🟡 需要 Unsplash Key；没配也返回 200，但 `photo_url` 是 `null` |

⚠️ 那几个「永远空数组」**不是你的密钥有问题**。`amap_service.py` 里的几个方法是
**桩代码**：它们**先成功调用了 MCP、把结果打印出来，然后无条件丢弃**。
例如 POI 搜索（`amap_service.py:166-167`）：

```python
print(f"POI搜索结果: {result[:200]}...")
return []                                  # ← 拿到了结果，却返回空列表
```

天气查询、`geocode`、`plan_route` 都是同一个套路。
**真实数据只通过 `/api/trip/plan` 那条链路流动。**

### 14.1 `/api/trip/health` 为什么必然 503

`trip.py:78-79`：

```python
"agent_name": agent.agent.name,
"tools_count": len(agent.agent.list_tools())
```

它访问的是 `agent.agent` —— 但 `MultiAgentTripPlanner` 上**没有** `.agent` 这个属性。
它只有四个 Agent：`attraction_agent`、`weather_agent`、`hotel_agent`、`planner_agent`。

于是 `AttributeError` → 被 `except` 捕获 → 返回 503。**这是代码里的一个真实 bug**，
大概率是从早期单 Agent 版本改成多 Agent 时漏改的。主流程不受影响，所以一直没被发现。

⚠️ **更糟的是**：`trip.py:73` 会先调用 `get_trip_planner_agent()`，
**把整套多智能体系统连 MCP 子进程一起初始化一遍**，然后才失败。
所以你点它会等十几秒到一分钟，最后拿到 503。
**别在它身上浪费时间，用 `GET /health`。**

**💭 思考题**：一个健康检查接口永远返回「服务不可用」，但主功能是好的。
这种 bug 为什么能长期存活？如果生产环境用它做存活探针，会发生什么？

（想改的话：把 `agent.agent` 换成 `agent.attraction_agent` 即可。**但先别改，
留着讲缺陷复盘。**）

### 14.2 `POST /api/map/route` 为什么必然 500

`amap_service.plan_route()` 直接 `return {}`，而 `RouteInfo` 要求四个必填字段
（`distance`、`duration`、`route_type`、`description`）。空字典过不了校验 →
`ValidationError` → 被捕获后转成 500。**同样不是你的配置问题。**

---

## 15. STEP4 报错速查表

| # | 现象 | 原因 | 怎么办 |
|---|---|---|---|
| E1 | `ModuleNotFoundError: No module named 'app'` | 不在 `backend/` 下运行 `run.py` | `cd backend` 再 `python run.py` |
| E2 | `ModuleNotFoundError: No module named 'fastapi'` | 虚拟环境没激活 | 提示符里要有 `(.venv)`，见 §2.2 |
| E3 | `ModuleNotFoundError: No module named 'huggingface_hub'` | 依赖不全 | `pip install "huggingface_hub>=0.25.0"` |
| E4 | 启动时 `❌ 配置验证失败` + `- AMAP_API_KEY未配置` | `.env` 里没填高德 Key | 见 §9。这是**硬性**要求 |
| E5 | 启动时 `⚠️ 配置警告` 提到 LLM | LLM Key 没配 | **能启动，但请求会失败**。补 `LLM_API_KEY` |
| E6 | `[Errno 10048] address already in use` | 8200 端口被占 | 改 `.env` 里的 `PORT=8201`，或杀掉占用进程（见下） |
| E7 | 浏览器打不开 `/docs` | 用了 `0.0.0.0` | 换成 `http://localhost:8200/docs` |
| E8 | **HTTP 422** | 请求体不合法 | 展开响应里的 `detail`，看 `loc` 指的字段。常见：写成了 `days`、`preferences` 给了字符串、`travel_days` 超出 1–30 |
| E9 | **HTTP 500** | Agent 执行时抛异常 | 看**终端**的堆栈，不是浏览器 |
| E10 | HTTP 503（`/api/trip/health`） | 代码 bug | 正常现象，见 §14.1 |
| E11 | 请求卡很久没反应 | 第一次请求要初始化 MCP | 等 1–3 分钟；同时看终端有没有在推进 |
| E12 | **200 但景点叫「北京景点1」** | 兜底假计划 | ⭐ 见 §12 |
| E13 | 终端出现 `⚠️ LLM rpm exhausted, retry n/3` | LLM 服务限流 | 框架会自动重试 3 次。偶发可忽略；频繁出现说明并发太高或套餐 rpm 太低 |
| E14 | 终端出现 `429 ... insufficient_quota` / `Allocated quota exceeded` | **LLM 账户额度用尽** | 去服务商后台充值或换 Key。⚠️ **接口仍会返回 200 + 兜底假计划**，不看终端根本发现不了 |
| E15 | 一堆 `FastAPIDeprecationWarning` | 框架对旧写法的提醒 | 正常，忽略 |
| E16 | 改了 `.env` 但没生效 | 配置在进程启动时读取 | 重启 `run.py`（改 `.env` 不会触发 reload） |
| E17 | 反复重启后越来越慢 | 残留的 `uvx` 子进程 | 见下方清理命令 |
| E18 | 用 `curl` 发中文请求体，秒回 `400 There was an error parsing the body` | 终端把中文编码搞坏了（Git Bash / cmd 常见） | **优先用 `/docs` 页面发请求**。非要用 curl 就把 JSON 存成 UTF-8 文件再 `--data-binary @文件名` |

查看谁占了 8200：

```powershell
netstat -ano | findstr :8200
```

清理残留的 MCP 子进程：

```powershell
Get-Process uvx -ErrorAction SilentlyContinue | Stop-Process -Force
```

---

## 16. STEP4 自检清单与思考题

### 自检清单

- [ ] `python 自检.py` 输出 `✅ 全部通过 (12/12)`
- [ ] `backend/.env` 里四项配置都填了
- [ ] `python run.py` 启动后看到 `✅ 配置验证通过`
- [ ] 浏览器能打开 `http://localhost:8200/docs`
- [ ] `POST /api/trip/plan` 返回 200 且 `"success": true`
- [ ] ⭐ **景点名是真实的（不是「北京景点1」）**
- [ ] 终端里看到了四个步骤的日志
- [ ] 能说清 `/api/trip/plan` 这个路径是哪三段拼出来的

**倒数第三条别跳过** —— 只满足 `success: true` 是不够的，兜底计划也满足。

### 💭 想清楚再走

1. **一个请求从进来到返回，经过了哪几层？** 每层负责什么？
2. **`request: TripRequest` 这一个标注同时做了哪三件事？**
3. **为什么 422 是 FastAPI 自动返回的，而 500 需要你去看终端？**
4. **`success: true` 能证明计划是真的吗？** 怎么判断？
5. **`/api/trip/health` 为什么坏了？** 为什么它坏得比 500 还贵？
6. **为什么高德 Key 缺失会让启动失败，而 LLM Key 缺失不会？**
7. **为什么 Agent 必须是单例？** 如果每个请求都新建一个会怎样？

答得上来就可以进入第二部分。

---

# 第二部分 · STEP5：前端字段穿越

---

## 17. 这一节要做什么

### 先说清楚：这一节不教 Vue

前端源码已经全部给你了，**不要求你会 Vue**。

这两个练习只有一个目的：**看懂一个值是怎么穿过 6 个文件的。**

这是全栈开发最真实的痛点 —— 不是哪个框架难学，而是
**一个字段要在多个地方保持一致，漏一处就断。**

| 练习 | 做什么 | 用时 |
|---|---|---|
| **B · 只读穿越** | 改一个默认值，观察它影响生成结果 | 15 分钟 |
| **A · 写入穿越** | 新增一个 `budget` 字段，要改 6 个地方 | 40 分钟 |

**先做 B 再做 A** —— B 简单，先建立「链路」的直觉。

### 17.1 这一节要开两个终端

这是本节最容易混乱的地方，**先把它理清**：

```text
┌─ 终端 A ──────────────────────────┐   ┌─ 终端 B ──────────────────────────┐
│ 位置: 教学包\backend               │   │ 位置: 教学包\frontend              │
│ 命令: python run.py                │   │ 命令: npm run dev                 │
│ 端口: 8200                         │   │ 端口: 5173                        │
│ 保持运行，不要关                    │   │ 保持运行，不要关                   │
└───────────────────────────────────┘   └───────────────────────────────────┘
              ▲                                        │
              └──────── 浏览器从 5173 调 8200 ──────────┘
```

**终端 A 就是第一部分那个，不用重开。** 只需要新开一个终端 B 给前端。

### 17.2 开始前请确认

- [ ] **第一部分的后端正在运行**，`http://localhost:8200/docs` 能打开
- [ ] `node -v` 输出 **v18 / v20 / v22 或更高**（见 §2.1）

### 17.3 先认识几个词

| 词 | 意思 |
|---|---|
| **Vite** | 前端开发服务器 + 打包工具。`npm run dev` 启的就是它 |
| **Vue 组件（.vue 文件）** | 一个文件里同时写模板（HTML）、逻辑（TypeScript）、样式（CSS） |
| **`reactive()`** | Vue 的响应式包装。数据一改，页面自动重新渲染 |
| **`v-model:value="x"`** | 双向绑定：用户在输入框里改 → `x` 跟着变；`x` 变 → 输入框显示也变 |
| **TypeScript / `interface`** | 给 JavaScript 加类型。`interface` 就是「这个对象该有哪些字段」的声明 |
| **axios** | 发 HTTP 请求的库 |
| **`sessionStorage`** | 浏览器里的临时存储。**按标签页隔离**，关掉标签页就没了 |
| **CORS** | 浏览器的跨域安全策略。网页在 5173，要调 8200 的接口，得由 8200 明确允许 |
| **ant-design-vue** | UI 组件库。`<a-select>`、`<a-col>` 这些 `a-` 开头的标签都来自它 |

---

## 18. 安装依赖并启动前端

### 18.1 打开终端 B，装依赖

```powershell
cd frontend
npm install
```

国内网络慢的话先换源（见 §2.5）。

`npm install` 会创建一个 `node_modules` 文件夹（几百 MB），第一次跑需要几分钟。

`package.json` 里声明的 7 个运行时依赖：

```json
"vue": "^3.5.13",
"vue-router": "^4.5.0",
"ant-design-vue": "^4.2.6",
"axios": "^1.7.9",
"@amap/amap-jsapi-loader": "^1.0.1",
"html2canvas": "^1.4.1",
"jspdf": "^3.0.3"
```

📌 代码实际还用了两个**没有在 `package.json` 里声明**的包：
`dayjs`（`Home.vue` 用它处理日期）和 `@ant-design/icons-vue`（`Result.vue` 用它的图标）。
它们目前能用，是因为 `ant-design-vue` 把它们作为自己的依赖装进来了、正好被提到了顶层。
**这是个隐患**（换成 pnpm 或者 ant-design-vue 升版就可能断），但本课不用处理。

### 18.2 配置前端 `.env`

前端也需要一个 `.env`（和后端那个是**两个不同的文件**）：

```powershell
copy .env.example .env
notepad .env
```

填成这样：

```ini
VITE_API_BASE_URL=http://localhost:8200
VITE_AMAP_WEB_JS_KEY=你的高德Web端JS API Key
```

📌 **注意是「Web 端（JS API）」Key，不是后端那个「Web 服务」Key。**
两者不能互换，**拿后端那个填进来，地图一定加载不出来。**

📌 代码里只读这两个变量（`api.ts:4` 和 `Result.vue:793`），`env.d.ts` 也只声明了这两个。
**没有多余的变量要填。**

📌 改完 `.env` 需要**重启** `npm run dev` 才生效 —— Vite 只在启动时读它。

⭕ 不配 `VITE_AMAP_WEB_JS_KEY` 也能做完两个练习，只是结果页地图空白。

### 18.3 启动

```powershell
npm run dev
```

期望输出：

```text
  VITE v6.3.6  ready in 823 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

浏览器打开 `http://localhost:5173/`，页面标题应该是 **「智能旅行助手」**。

**先完整提交一次**（填城市和日期 → 生成 → 跳到结果页），确认前后端联通了，再开始做练习。

---

## 19. ⚠️ 最容易翻车的一个坑：端口 5173

**请务必看清 Vite 打印的端口号。**

`vite.config.ts` 里指定了 `port: 5173`，但**没有**设 `strictPort`。所以如果 5173 已经被占用，
**Vite 会静默地改用 5174**，只在那行 `Local:` 里体现出来：

```text
  ➜  Local:   http://localhost:5174/     ← 注意！变成 5174 了
```

**后果**：后端的 CORS 白名单里只有 5173，没有 5174。于是页面能打开、表单能填，
但一点「生成」就失败，而且**报错完全看不出是 CORS 问题**（浏览器只会说网络错误）。

### 怎么处理

**方案一（推荐）**：找出占用 5173 的进程并关掉。

```powershell
netstat -ano | findstr :5173
```

**方案二**：把新端口加进后端的 CORS 白名单。改 `backend/.env`：

```ini
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://localhost:3000
```

然后**重启后端**（改 `.env` 不会触发自动重载）。

### 同类问题：localhost 和 127.0.0.1 不通用

它们在 CORS 眼里是**两个不同的源**。
`.env.example` 的默认值里四项都有（`localhost` 和 `127.0.0.1` 各两个），所以两种写法都能用 ——
**但前提是你没有把这一行改短。**

**建议统一用 `localhost`**，省心。

---

## 20. 前端代码在做什么

### 20.1 `main.ts` —— 应用入口

```typescript
const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/',       name: 'Home',   component: Home },
    { path: '/result', name: 'Result', component: Result }
  ]
})

const app = createApp(App)
app.use(router)
app.use(Antd)
app.mount('#app')
```

- 两个页面：`/` 是首页表单，`/result` 是结果页
- `app.use(Antd)` 全局注册 UI 组件库 —— 这就是为什么模板里能直接写 `<a-select>`
- `import 'ant-design-vue/dist/reset.css'` 是样式重置，**少了这行页面会变得很难看**

### 20.2 `Home.vue` —— 首页表单

**关键概念：`formData` 和 `requestData` 是两个不同的东西。**

`formData` 是**页面状态**（`Home.vue:224-233`）：

```typescript
const formData = reactive<TripFormState>({
  city: '',
  start_date: null,
  end_date: null,
  travel_days: 1,
  transportation: '公共交通',
  accommodation: '经济型酒店',
  preferences: [],
  free_text_input: ''
})
```

`requestData` 是**要发给后端的请求体**（`Home.vue:280-289`）：

```typescript
const requestData: TripFormData = {
  city: formData.city,
  start_date: formData.start_date.format('YYYY-MM-DD'),
  end_date: formData.end_date.format('YYYY-MM-DD'),
  travel_days: formData.travel_days,
  transportation: formData.transportation,
  accommodation: formData.accommodation,
  preferences: formData.preferences,
  free_text_input: formData.free_text_input
}
```

**为什么要分成两个？** 看 `start_date` 就明白了：

- 页面上的日期选择器给的是一个 `Dayjs` 对象（能做日期运算，比如自动算出天数）
- 后端要的是字符串 `"2025-06-01"`

所以 `requestData` 这一步在做**格式转换**。**`formData` 服务于 UI，`requestData` 服务于 API。**

📌 这也解释了 `TripFormState` 这个类型的存在（`Home.vue:219-222`）：

```typescript
type TripFormState = Omit<TripFormData, 'start_date' | 'end_date'> & {
  start_date: Dayjs | null
  end_date: Dayjs | null
}
```

意思是「和 `TripFormData` 一样，**除了**两个日期字段换成 `Dayjs` 类型」。

⚠️ **`TripFormState` 定义在 `Home.vue` 里，不在 `types/index.ts` 里。** 别去那边找。

⭐ 而且因为它是从 `TripFormData` 派生的（`Omit<...>`），**你给 `TripFormData` 加字段时，
`TripFormState` 会自动跟着有。** 练习 A 里你不需要手动改它。

提交成功后（`Home.vue:297-306`）：

```typescript
if (response.success && response.data) {
  sessionStorage.setItem('tripPlan', JSON.stringify(response.data))
  message.success('旅行计划生成成功!')
  setTimeout(() => { router.push('/result') }, 500)
}
```

注意 `response.data` —— `generateTripPlan` 返回的是 HTTP 响应体
`{success, message, data}`，所以 `.data` 才是 `TripPlan`（这就是 §5.2 讲的那层包装）。

### 20.3 `services/api.ts` —— 发请求

```typescript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8200'

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 120000, // 2分钟超时
  headers: { 'Content-Type': 'application/json' }
})

export async function generateTripPlan(formData: TripFormData): Promise<TripPlanResponse> {
  try {
    const response = await apiClient.post<TripPlanResponse>('/api/trip/plan', formData)
    return response.data
  } catch (error: any) {
    console.error('生成旅行计划失败:', error)
    throw new Error(error.response?.data?.detail || error.message || '生成旅行计划失败')
  }
}
```

三个要留意的点：

**① `timeout: 120000` 是 2 分钟的硬上限。** 如果 LLM 很慢（尤其第一次请求还要初始化 MCP），
请求会被 axios 掐断，而页面上的进度条正卡在 90%。看起来像「卡死了」，其实是超时。

**② 那两个拦截器是你唯一能看到真实状态码的地方。**

```typescript
console.error('响应错误:', error.response?.status, error.message)
```

按 **F12** 打开 DevTools → Console，出错时这里会打印真实的 HTTP 状态码。
**页面上弹出的红色提示信息往往没有这个有用。**

**③ `vite.config.ts` 里那段 proxy 配置其实从未生效。** 因为 axios 用的是绝对地址
`http://localhost:8200`，请求直接从浏览器发到 8200，**不经过 Vite 的开发服务器**。
真正让跨域请求成立的是**后端的 CORS 配置** —— 这也是为什么 §19 那个坑那么关键。

### 20.4 `types/index.ts` —— 前后端的合同

```typescript
export interface TripFormData {     // 第 79 行
  city: string
  start_date: string
  end_date: string
  travel_days: number
  transportation: string
  accommodation: string
  preferences: string[]
  free_text_input: string
}
```

**这个 interface 必须和后端的 `TripRequest` 对齐。** 练习 A 就是要改它。

### 20.5 ⚠️ `budget` 这个名字已经被占用了 —— 练习 A 最大的陷阱

`types/index.ts` 里**已经有一个 `budget`**，但它和练习 A 要加的那个**完全无关**：

```typescript
// types/index.ts:40-46 —— 已存在
export interface Budget {
  total_attractions: number
  total_hotels: number
  total_meals: number
  total_transportation: number
  total: number
}

// types/index.ts:69-77 —— 已存在
export interface TripPlan {
  ...
  budget?: Budget          // ← 第 76 行。这是【输出】：LLM 算出来的费用明细
}
```

对比一下：

| | 已存在的 `TripPlan.budget` | 练习 A 要加的 `TripFormData.budget` |
|---|---|---|
| 位置 | `types/index.ts:76` | `types/index.ts:79` 那个接口里 |
| 方向 | **输出**（后端 → 前端） | **输入**（前端 → 后端） |
| 类型 | `Budget` 对象（5 个数字） | `string`（`"经济"` / `"中等"` / `"宽松"`） |
| 含义 | 花了多少钱 | 用户想花多少钱的档位 |
| 谁用它 | `Result.vue` 渲染预算卡片 | Planner 提示词 |

🚨 **所以：在 `types/index.ts` 里搜 "budget" 会先跳到错的那个。**
你要改的是 **`TripFormData`**，**不要**动 `TripPlan` 或 `Budget` ——
改错了会**静默搞坏结果页的预算卡片**。

### 20.6 `Result.vue` —— 结果页

页面加载时从 `sessionStorage` 取数据：

```typescript
const data = sessionStorage.getItem('tripPlan')
if (data) {
  tripPlan.value = JSON.parse(data)
}
```

也就是说，首页**不是**通过 URL 参数传递整个行程，而是先暂存进浏览器。

⚠️ **`sessionStorage` 是按标签页隔离的。** 所以在新标签页里直接打开
`http://localhost:5173/result`，会看到「没有找到旅行计划数据」的空状态。
**这不是程序坏了** —— 必须从首页提交一次才有数据。

📌 `Result.vue:435` 加载景点图片时把后端地址**写死**了：

```typescript
fetch(`http://localhost:8200/api/poi/photo?name=${encodeURIComponent(attraction.name)}`)
```

它无视 `VITE_API_BASE_URL`。所以改 `.env` 里的后端地址，图片这一块不会跟着变。

### 20.7 进度条是假的

`Home.vue:262-277` 用一个定时器每 500 毫秒加 10%，**封顶 90%**：

```typescript
const progressInterval = setInterval(() => {
  if (loadingProgress.value < 90) {
    loadingProgress.value += 10
    ...
```

它**完全不反映后端的真实进度**。那些「🔍 正在搜索景点...」的文字也是按百分比编的，
和后端到了哪一步毫无关系。

叠加 §20.3 那个 2 分钟超时，一个慢请求的表现就是：进度条冲到 90% 卡住，
过一会儿弹出失败。**要看真实进度，去看终端 A 的后端日志。**

---

## 21. 练习 B · 只读穿越（15 分钟）

目标：改一个默认值，让它一路走到 LLM 那里并影响输出。

### 21.1 动手前先预测

改动只有一行。**在动手之前**，先在纸上写下你的预测：

> 这个值会经过哪几个文件？按顺序列出来。

（写完再往下看，不要偷看答案。）

### 21.2 改一处

打开 `frontend/src/views/Home.vue`，找到 `formData`（**第 224 行**开始），
把最后一个字段 `free_text_input`（**第 232 行**）的默认值从空字符串改成一句话：

```typescript
const formData = reactive<TripFormState>({
  city: '',
  start_date: null,
  end_date: null,
  travel_days: 1,
  transportation: '公共交通',
  accommodation: '经济型酒店',
  preferences: [],
  free_text_input: '每天安排一个博物馆'      // ← 改这里
})
```

保存。Vite 会自动热更新，**不用重启**。

### 21.3 ⚠️ 不要改 `placeholder`

`Home.vue` 里那个 `<a-textarea>` 有个 `placeholder` 属性。

**`placeholder` 只是输入框为空时显示的灰色提示文字，它永远不会进入请求体。**
改它你会看到页面变了，但生成结果完全不受影响 —— **这是本练习最常见的错误。**

### 21.4 验证

1. 刷新 `http://localhost:5173/`
2. 「额外要求」输入框里应该**已经填着**「每天安排一个博物馆」
   （**是真实内容，不是灰色提示** —— 你可以选中它、删掉它）
3. 填上城市和日期，提交
4. **验收标准**：生成的行程里真的多出了博物馆

### 21.5 对答案：这个字段走过了哪些站

```text
Home.vue   formData.free_text_input          ← 你改的地方（第 232 行）
    │
    ▼
Home.vue   requestData.free_text_input       ← 拷进请求体（第 288 行）
    │
    ▼
api.ts     generateTripPlan()                ← axios 发出 POST /api/trip/plan
    │
    ▼  HTTP
    ▼
schemas.py TripRequest.free_text_input       ← 后端接收并校验（第 19 行）
    │
    ▼
trip_planner_agent.py  _build_planner_query()   ← ⭐ 第 325-326 行
           if request.free_text_input:
               query += f"\n**额外要求:** {...}"
    │
    ▼
Planner Agent 的提示词 → LLM → TripPlan
    │
    ▼
Result.vue 渲染
```

**七个站点，一个都不能少。**

你预测对了几站？**第 5 站最容易被漏掉** —— 很多人以为数据到了后端就结束了，
但它还要再走进提示词，才能真正影响 LLM。

📌 补充一个细节：`free_text_input` **不是**拼在「基本信息」块里的，而是作为一个
**独立的 `**额外要求:**` 段落**追加在提示词末尾。练习 A 的 `budget` 才是加进「基本信息」块。

**这也解释了练习 A 为什么要改 6 个地方**：`free_text_input` 之所以只改一处就通了，
是因为其余六站**早就铺好了**。

---

## 22. 练习 A · 写入穿越（40 分钟）

### 题目

> 课本文档说，请求里有一个 `budget` 预算字段。
> 但你翻遍代码，会发现**根本没有这个字段**（§3.4 的差异表里列了这一条）。
> **请把它补上。**

这不是编出来的习题 —— 这是课本文档和代码脱节留下的**真实缺口**。你要做的是一件真事。

要改 **6 个地方**，跨越 3 种语言（Vue 模板 / TypeScript / Python）、2 个进程、1 次网络传输。

### 📋 改动总览

**漏掉任何一处，都会有一个对应的现象。每个现象都在告诉你链路断在哪一站。**

| 第几处 | 文件 | 改什么 | 漏掉的后果 |
|---|---|---|---|
| **1** | `types/index.ts` | `TripFormData` 加 `budget: string` | 第 2、4 处报 TS 类型错误 |
| **2** | `Home.vue:224-233` | `formData` 加默认值 `budget: '中等'` | 下拉框没有预选值（空框） |
| **3** | `Home.vue` 偏好设置区块 | 模板里加一个下拉框 | 用户没法选 |
| **4** | `Home.vue:280-289` | `requestData` 加 `budget` | **选了也不会发给后端** |
| **5** | `schemas.py` | `TripRequest` 加 `budget` | 后端**静默丢弃**这个字段 |
| **6** | `trip_planner_agent.py` | 提示词里加一行 | 接口成功，但**行程完全不变** |

⚠️ **顺序和你想的可能不一样：类型声明必须第一个改。**
因为 `formData` 和 `requestData` 的类型都来自 `TripFormData`，
类型里没有 `budget` 的话，第 2、4 步会直接报「对象字面量中有多余属性」的错误。

---

### 第 1 处 · `types/index.ts`（先做这一步）

⚠️ 再提醒一次：改 **`TripFormData`**（第 79 行），**不是** `TripPlan`，**不是** `Budget`。
（原因见 §20.5。）

```typescript
export interface TripFormData {
  city: string
  start_date: string
  end_date: string
  travel_days: number
  transportation: string
  accommodation: string
  budget: string              // ← 新增
  preferences: string[]
  free_text_input: string
}
```

📌 **不需要**改 `TripFormState` —— 它是 `Omit<TripFormData, ...>`，会自动继承这个新字段。

---

### 第 2 处 · `Home.vue` 的 `formData`

```typescript
const formData = reactive<TripFormState>({
  city: '',
  start_date: null,
  end_date: null,
  travel_days: 1,
  transportation: '公共交通',
  accommodation: '经济型酒店',
  budget: '中等',              // ← 新增
  preferences: [],
  free_text_input: ''
})
```

这一步的作用是给一个**默认值**，让下拉框打开时就显示「中等」。

📌 **常见误解澄清**：有些资料说「不加这一步下拉框会点了没反应」。**在 Vue 3 里不会。**
`reactive()` 基于 Proxy，`v-model:value="formData.budget"` 会在运行时自动新增并追踪这个键，
下拉框照样能选能显示。漏掉这一步的真实后果只有两个：
①打开时是**空框**，没有预选值 ②`npm run build` 时 TypeScript 会报错。

---

### 第 3 处 · `Home.vue` 的模板（抄现成的）

找到「偏好设置」那个区块（`Home.vue:94` 开始）。它的结构是：

```text
<div class="form-section">
  <div class="section-header"> ⚙️ 偏好设置 </div>

  <a-row :gutter="24">
    <a-col :span="8"> ... 交通方式 ...  </a-col>    ← 第 100 行
    <a-col :span="8"> ... 住宿偏好 ...  </a-col>    ← 第 113 行
    <a-col :span="8"> ... 旅行偏好 ...  </a-col>    ← 第 126 行
  </a-row>                                          ← 新的插在这之前
</div>
```

**最省事的做法：复制「交通方式」那一整个 `<a-col>`**（第 100–112 行，
**注意要连 `<a-col>` 和 `</a-col>` 一起复制**，只复制里面的 `<a-form-item>` 是不行的），
然后改成预算：

```vue
<a-col :span="6">
  <a-form-item name="budget">
    <template #label>
      <span class="form-label">预算档位</span>
    </template>
    <a-select v-model:value="formData.budget" size="large" class="custom-select">
      <a-select-option value="经济">💰 经济</a-select-option>
      <a-select-option value="中等">💵 中等</a-select-option>
      <a-select-option value="宽松">💎 宽松</a-select-option>
    </a-select>
  </a-form-item>
</a-col>
```

**插入位置**：最后一个 `</a-col>` 之后、`</a-row>` 之前
（也就是三个现有列的后面，但**还在 `<a-row>` 里面**）。

解释：

- `v-model:value="formData.budget"` —— 把这个下拉框和 `formData.budget` 双向绑定
- **`value="经济"` 才是真正提交的数据**，`💰 经济` 只是显示给人看的文字
- `<a-form-item>` 提供标签和布局，`<a-col>` 提供栅格列

#### ⚠️ 关于 `:span` 的数字

ant-design-vue 的栅格一行总共 **24** 格。现有三列都是 `:span="8"`，
3 × 8 = 24 **已经占满一整行**。

所以如果你的新列也写 `:span="8"`，它会被挤到**第二行**去（能用，只是不好看）。

**两个选择**：

- 用 `:span="6"`，然后**把原来三个 `:span="8"` 也都改成 `:span="6"`** → 四列均分一行（推荐）
- 直接用 `:span="8"` → 换行，不影响功能

#### 页面白屏了怎么办

模板标签写坏了会导致整页空白。逐项检查：

- `<a-col>` 有没有配对的 `</a-col>`
- `<a-form-item>` 有没有配对的 `</a-form-item>`
- 新的 `<a-col>` 是不是**在 `<a-row>` 里面**（不能放在 `</a-row>` 外面）

按 **F12** → Console，Vue 的模板编译错误会打印在那里，通常直接指出哪一行。

---

### 第 4 处 · `Home.vue` 的 `requestData`

找到 `handleSubmit` 里构造 `requestData` 的地方（第 280 行开始），加一行：

```typescript
const requestData: TripFormData = {
  city: formData.city,
  start_date: formData.start_date.format('YYYY-MM-DD'),
  end_date: formData.end_date.format('YYYY-MM-DD'),
  travel_days: formData.travel_days,
  transportation: formData.transportation,
  accommodation: formData.accommodation,
  budget: formData.budget,          // ← 新增
  preferences: formData.preferences,
  free_text_input: formData.free_text_input
}
```

⭐ **前三处只是让页面里「有」这个值，这一步才真正把它发出去。**

这是最容易漏的一处，因为页面上看起来一切正常 —— 下拉框能选、能显示，
但请求体里根本没有它。

**怎么确认它发出去了**：F12 → Network → 点那个 `plan` 请求 → Payload / 请求负载，
看有没有 `"budget": "经济"`。

---

### 第 5 处 · 后端 `schemas.py`

文件：`backend/app/models/schemas.py`

在 `accommodation` 后面加一行：

```python
class TripRequest(BaseModel):
    """旅行规划请求"""
    city: str = Field(..., description="目的地城市", example="北京")
    start_date: str = Field(..., description="开始日期 YYYY-MM-DD", example="2025-06-01")
    end_date: str = Field(..., description="结束日期 YYYY-MM-DD", example="2025-06-03")
    travel_days: int = Field(..., description="旅行天数", ge=1, le=30, example=3)
    transportation: str = Field(..., description="交通方式", example="公共交通")
    accommodation: str = Field(..., description="住宿偏好", example="经济型酒店")
    budget: str = Field(default="中等", description="预算档位", example="中等")   # ← 新增
    preferences: List[str] = Field(default=[], description="旅行偏好标签", example=["历史文化", "美食"])
    free_text_input: Optional[str] = Field(default="", description="额外要求", example="希望多安排一些博物馆")
```

顺手把下面 `json_schema_extra` 的示例也补上，这样 `/docs` 里的预填内容才完整：

```python
            "accommodation": "经济型酒店",
            "budget": "中等",
```

#### ⚠️ 漏掉这一处会发生什么（一个重要的澄清）

你可能听说过「漏掉这一步会返回 HTTP 422」。**在本项目里不会。**

`TripRequest` 没有配置 `extra="forbid"`，而 Pydantic 2 的默认行为是 `extra='ignore'` ——
**多余的字段被静默丢弃**。所以：

- HTTP 状态码：**200**
- `success`：**true**
- 但 `budget` **凭空消失**，后端根本不知道你发过它
- **没有任何报错**

**这比 422 难查得多**，因为你必须自己去比对「Network 里发出去了」和「后端行为没变化」。

（如果你真的遇到了 422，那是**别的**字段不匹配 —— 检查 `travel_days` 是不是超出 1–30、
`preferences` 是不是写成了字符串、日期格式对不对。）

---

### 第 6 处 · Planner 提示词 ★

文件：`backend/app/agents/trip_planner_agent.py`

找到 `_build_planner_query()`（**第 296 行**）里的「基本信息」块（**第 300–306 行**），加一行：

```python
**基本信息:**
- 城市: {request.city}
- 日期: {request.start_date} 至 {request.end_date}
- 天数: {request.travel_days}天
- 交通方式: {request.transportation}
- 住宿: {request.accommodation}
- 预算档位: {request.budget}                    ← 新增
- 偏好: {', '.join(request.preferences) if request.preferences else '无'}
```

⭐ **前五处只是让预算「到达后端」。这一步才让它真正进入 LLM 的视野。**

漏掉这一处时：接口成功、字段也确实传到了后端、`request.budget` 里也有值 ——
**但生成的行程完全不会按预算变化**，因为 LLM 从头到尾没看见过它。

**这是本练习最重要的一个认知**：
**数据传到后端 ≠ 产生了影响。对 LLM 应用来说，「进入提示词」才是真正的生效点。**

---

## 23. 改完怎么验证

### 23.1 重启后端

第 5、6 处改的是 Python 文件。`run.py` 开了 `reload=True`，理论上会自动重启，
但**建议手动重启一次**确保干净：终端 A 按 `Ctrl+C`，然后 `python run.py`。

### 23.2 前端类型检查

⚠️ **`npm run dev` 完全不做类型检查。** Vite 用 esbuild 直接把类型剥掉，
所以「TypeScript 报错」在浏览器里**永远看不到**。

要检查类型，必须跑构建：

```powershell
npm run build
```

这条命令等于 `vue-tsc && vite build` —— 先做全量类型检查，再打包。
成功的话会生成一个 `dist/` 目录，并打印类似 `✓ built in 8.5s`。

如果它报错，最可能是第 1 处（`types/index.ts`）没改，或者改到了 `TripPlan` 上。

📌 `tsconfig.json` 开了 `strict`、`noUnusedLocals`、`noUnusedParameters`，
所以**未使用的变量也会被当成错误**。

📌 另外 `tsconfig.json` 只检查 `src/**/*`，所以 `vite.config.ts` 里的错误构建时查不出来。

### 23.3 页面验证

打开 `http://localhost:5173/`，**分别用「经济」和「宽松」各提交一次**，
**城市和日期保持一致**。

**验收标准**：两次生成的行程，**酒店档次和各项预估费用**能看出方向性的差别。

- 「经济」→ 倾向更便宜的酒店、餐饮、门票
- 「宽松」→ 允许更高档的酒店和更丰富的安排

📌 结果页**不会**显示你选的档位本身，它只渲染 LLM 算出来的费用明细
（`TripPlan.budget` 那个对象，见 §20.5）。**所以别去找「经济」这两个字，看酒店和钱数。**

📌 LLM 有随机性，差异**不会每次都很戏剧化**。重点是能观察到方向性的区别。

### 23.4 如果两次完全没有差别

按这个顺序查：

1. **第 6 处** —— 提示词里到底有没有 `- 预算档位: {request.budget}`？
2. **第 4 处** —— F12 → Network → Payload 里有 `budget` 吗？
3. **第 5 处** —— `TripRequest` 里加了吗？后端重启了吗？

### 23.5 顺便再跑一次后端自检

```powershell
python .\自检.py
```

`budget -> (无此字段)` 那一行现在应该变成 `⚠️ 已被添加` ——
**这说明你第 5 处改对了。** 整体仍然是 12/12 通过。

---

## 24. STEP5 报错速查表

### 按现象定位在哪一层断了

| 现象 | 断在哪一层 | 查第几处 |
|---|---|---|
| **页面白屏** | 前端渲染层 | 第 3 处，模板标签没配对 |
| **下拉框是空的**（没有预选值） | 前端数据层 | 第 2 处 |
| **`npm run build` 报类型错** | 类型契约层 | 第 1 处（或改到了 `TripPlan` 上） |
| **Network 里请求体没有 `budget`** | 前端提交层 | 第 4 处 |
| **发出去了，但后端行为没变**（200，无报错） | 前后端契约层 | 第 5 处（字段被静默丢弃）← **最难查** |
| **接口成功，但行程无变化** | 提示词层 | 第 6 处 ← **最容易漏** |

### 具体报错

| # | 现象 | 原因 | 怎么办 |
|---|---|---|---|
| E1 | `npm run dev` 起不来，提示 Node 版本 | Node 16/17/19/21 | 升到 18 / 20 / 22，见 §2.1 |
| E2 | 页面能打开，一提交就失败，报错看不懂 | ⭐ Vite 用了 5174，或你用了 `127.0.0.1` 但 CORS 里没有 | **见 §19**，这是最常见的一个 |
| E3 | 页面弹出 `[object Object]` | FastAPI 的 422 `detail` 是数组，被直接塞进了错误提示 | F12 → Console 看 `响应错误: 422 ...`，或 Network → Response 看真实内容 |
| E4 | 进度条卡在 90% 然后失败 | axios 2 分钟超时到了（第一次请求要初始化 MCP，很慢） | 看终端 A 的日志确认后端还在跑。第二次请求会快很多 |
| E5 | 结果页地图空白 | `VITE_AMAP_WEB_JS_KEY` 没配、配错、或用了「Web 服务」Key | 见 §18.2。**必须是「Web 端（JS API）」类型** |
| E6 | 景点没有图片 | 依赖 Unsplash，境外服务可能不通 | 不影响主流程，可以忽略 |
| E7 | 直接打开 `/result` 显示「没有找到旅行计划数据」 | `sessionStorage` 按标签页隔离 | 正常。从首页提交一次 |
| E8 | 改了代码页面没变 | 极少数情况热更新失效 | `Ctrl+C` 后重新 `npm run dev`；浏览器 `Ctrl+Shift+R` 强制刷新 |
| E9 | `npm install` 卡住 | 网络 | `npm config set registry https://registry.npmmirror.com` |
| E10 | 修改 `.env` 后不生效 | Vite 只在启动时读 `.env` | 重启 `npm run dev` |
| E11 | 行程里景点叫「北京景点1」 | 后端兜底假计划 | 不是前端的问题，回 §12 看终端 |

---

## 25. STEP5 自检清单与思考题

### 自检清单

- [ ] `node -v` ≥ 18
- [ ] 终端 A 的后端在跑，`/docs` 能打开
- [ ] 终端 B 的 `npm run dev` 起来了，**并且确认了端口就是 5173**
- [ ] 浏览器 `http://localhost:5173/` 能看到「智能旅行助手」
- [ ] 能提交一次并跳转到结果页看到行程
- [ ] 练习 B：`free_text_input` 默认值影响了生成结果（且你没有去改 `placeholder`）
- [ ] 练习 B：能说出这个字段走过的 7 站
- [ ] 练习 A：6 处全部改完
- [ ] `npm run build` 通过，生成了 `dist/`
- [ ] F12 → Network 里确认请求体含 `"budget"`
- [ ] 「经济」和「宽松」两次生成有可观察的差异

### 💭 想清楚再走

1. **`formData` 和 `requestData` 有什么区别？** 为什么要分成两个？
2. **`TripFormData` 为什么必须和后端 `TripRequest` 对齐？** 不对齐会怎样？
   （提示：**不是** 422）
3. **为什么数据到了后端还不够，必须进到 Planner 提示词？**
4. **为什么一个字段要改 6 个地方？** 它跨越了几种语言、几个进程？
5. **这 6 处里哪些能自动化掉？**

### 第 5 题的答案值得知道

**能。** FastAPI 可以导出 OpenAPI schema，用 `openapi-typescript` 之类的工具
自动生成前端类型 —— **第 1 处就能省掉**，而且前后端类型**永远不会漂移**。
真实项目里常这么做。

### 最后回看一遍

```text
数据模型（Pydantic）  ── 定义数据长什么样，拦住 LLM 的胡说
多智能体流水线        ── 把任务拆开，四个 Agent 顺序协作
MCP 工具接入          ── 让 Agent 真的能查到外部数据
FastAPI 串联（STEP4） ── 变成一个能被调用的服务
前端字段穿越（STEP5） ── 看清一个字段的完整生命周期
```

那些「看起来只是类型注解」的 Pydantic 模型，在 STEP4 变成了**接口契约**，
在 STEP5 变成了前端的 **TypeScript interface**。**这条线是整门课的主干。**

**一个字段，要在 6 个地方保持一致 —— 这就是全栈开发最真实的成本。**
它跟「框架难不难学」没关系：即使你把 Vue、FastAPI、Pydantic 都学透了，
加一个字段还是得改 6 个地方。

---

# 附录 A：课本文档与实际代码的差异表

⚠️ **课本 `ch13.md` 里的代码和你手上这份不一致。照课本敲会卡住，而且报错信息
往往看不出真正原因。以你手上的代码为准。**

| 项 | 课本文档写的 | 实际代码 |
|---|---|---|
| 请求模型名 | `TripPlanRequest` | **`TripRequest`** |
| 天数字段 | `days` | **`travel_days`** |
| 偏好字段 | `preferences: str` | **`preferences: List[str]`**（字符串列表） |
| 预算输入 | 有 `budget` | **请求里没有这个字段**（只在输出 JSON 里有）。STEP5 练习 A 就是把它补上 |
| 额外要求 | 无 | 多了 **`free_text_input`** |
| MCP 启动方式 | `npx @sugarforever/amap-mcp-server` | **`uvx amap-mcp-server`** |
| MCPTool 参数名 | `command` + `args` | **`server_command`** |
| 高德环境变量 | `AMAP_API_KEY` | `.env` 里填 `AMAP_API_KEY`，但传给 MCP 子进程时是 **`AMAP_MAPS_API_KEY`**（多了 `MAPS`） |
| Agent 类名 | `TripPlannerAgent` | **`MultiAgentTripPlanner`** |
| 页面跳转传参 | `router.push({state})` | **`sessionStorage`** + `router.push('/result')` |
| 响应结构 | 直接返回 `TripPlan` | 外面包了一层 **`{success, message, data}`** |
| `Attraction` 字段 | 无 | 多了 **`photos`**、**`poi_id`**、**`image_url`**（给前端加载配图用） |

另外，课本 `ch13.md:568` 的示例代码本身有语法错误
（`SimpleAgent(name="景点搜索"prompt=...)` 缺一个逗号），照敲必报错。

---

# 附录 B：讲师备课要点

## B1 建议时间分配

| 时段 | 内容 | 用时 |
|---|---|---|
| 课前 | §2 环境准备 + 申请密钥（学员自行完成） | — |
| 0:00–0:10 | 开场：整体架构 + 今天要做什么（§1） | 10 min |
| 0:10–0:30 | §3 先认识手上这份代码（三层结构、四个 Agent、MCP） | 20 min |
| 0:30–0:40 | §5 请求走过的路 + §6 一个模型三件事 | 10 min |
| 0:40–0:50 | §8 自检 + §9 配 `.env` | 10 min |
| 0:50–1:15 | §10 §11 启动 + `/docs` 发第一个请求 ⭐**里程碑** | 25 min |
| 1:15–1:35 | §12 真假计划 + §14 缺陷复盘 | 20 min |
| — | **休息** | 10 min |
| 1:45–2:05 | §18 启动前端 + §20 代码讲解 | 20 min |
| 2:05–2:20 | §21 练习 B | 15 min |
| 2:20–3:00 | §22 练习 A | 40 min |
| 3:00–3:15 | §23 验收 + §25 总结与思考题 | 15 min |

**总计约 3 小时**（含休息）。

## B2 三个必须守住的里程碑

1. **`自检.py` 12/12 通过**（无密钥，人人能过）—— 先建立「装配是对的」的信心
2. **`/docs` 拿到一份真实行程 JSON** —— 这是 STEP4 的里程碑，**不能被琐碎问题拖垮**
3. **练习 A 六处改完并看到行程变化** —— 这是 STEP5 的里程碑

## B3 发放节奏

- **§2 环境准备必须课前 3 天发**，并盯着学员申请密钥（尤其**高德两个 Key**）
- **§22 练习 A 的分步操作建议先不给**，让学员自己预测「要改几个地方」，
  再对答案 —— 这个练习的价值在于**预测和现实的差距**
- **§21.1 的「动手前先预测」不要跳过**

## B4 最高频的五个翻车点

| 排名 | 翻车点 | 预防 |
|---|---|---|
| 1 | **高德 Key 类型选错**（Web 服务 ↔ Web 端 JS API） | §2.6 反复强调，症状是「工具能加载但查询全失败」 |
| 2 | **Vite 静默换到 5174** → CORS 失败但报错看不出 | §19，让学员**念出**终端打印的端口号 |
| 3 | **虚拟环境没激活** → `ModuleNotFoundError` | 让学员先看提示符有没有 `(.venv)` |
| 4 | **LLM 额度用尽** → 200 + 兜底假计划，无报错 | §12，教会看景点名 |
| 5 | **练习 A 改到了 `TripPlan.budget`** | §20.5，发放前先讲这个陷阱 |

## B5 可以留作讨论的三个缺陷

这三个都是代码里的**真实缺陷**，教学价值很高，建议**保留不修**：

| 缺陷 | 位置 | 讨论点 |
|---|---|---|
| **天气查错了日期** | `trip_planner_agent.py:250` | 数据看起来完全正常，但语义是错的。这类 bug 怎么发现？ |
| **健康检查永远 503** | `trip.py:78-79` | 一个坏掉的健康检查为什么能长期存活？用它做存活探针会怎样？ |
| **兜底计划静默返回** | `trip_planner_agent.py:281` | 「永不失败」的设计代价是什么？什么时候该让错误暴露出来？ |

另外两个可讲的：`amap_service.py` 的桩代码（拿到结果却 `return []`）、
`Home.vue` 的假进度条。

---

# 附录 C：两个通用报错

## C1 中文显示成乱码

Windows 终端默认不是 UTF-8。先切一下：

```powershell
chcp 65001
```

## C2 `UnicodeEncodeError: 'gbk' codec can't encode character '\u2705'`

这个错**只在你把输出重定向或管道转发**时出现，例如：

```powershell
python 自检.py > 输出.txt
```

原因：`chcp 65001` 只改**控制台**的代码页；一旦输出不是直接进控制台，
Python 会改用系统的 ANSI 代码页（中文 Windows 上是 cp936/GBK），而 GBK 编不出 `✅`。

所以 **`chcp 65001` 修不了这个错**。正确解法是让 Python 全程用 UTF-8：

```powershell
$env:PYTHONUTF8=1
python 自检.py > 输出.txt
```

或者单次生效：

```powershell
python -X utf8 自检.py > 输出.txt
```

在普通交互式终端里直接跑脚本**不会**触发这个错，不用担心。
