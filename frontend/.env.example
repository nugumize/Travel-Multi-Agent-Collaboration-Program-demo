# ============================================================
# 前端配置模板
# ============================================================
# 用法: 复制本文件为 .env, 然后填入你自己的 Key。
#     copy .env.example .env
#
# ⚠️ 改完需要重启 npm run dev 才生效 —— Vite 只在启动时读 .env。
# ⚠️ .env 里是你的真实 Key, 不要提交到代码仓库。
# ============================================================

# 后端 API 地址。要和后端实际监听的端口一致(见 backend/.env 的 PORT)。
VITE_API_BASE_URL=http://localhost:8200

# 高德地图【Web 端(JS API)】Key —— 结果页画地图用这个。
# ⚠️ 注意类型: 这里要的是"Web 端(JS API)", 不是后端那个"Web 服务"。
#    两者不能互换, 填错了地图加载不出来。
#    申请: https://console.amap.com/  添加 Key 时"服务平台"选"Web 端(JS API)"
#
# 🔒 前端 Key 会暴露在浏览器里(这是 Web 端 Key 的设计), 但务必在高德控制台
#    给它配置域名白名单, 否则额度会被别人刷。
VITE_AMAP_WEB_JS_KEY=your_amap_web_js_key_here
